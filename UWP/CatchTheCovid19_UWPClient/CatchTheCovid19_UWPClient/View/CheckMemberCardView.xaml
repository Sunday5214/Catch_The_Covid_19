<Page
    x:Class="CatchTheCovid19_UWPClient.View.CheckMemberCard"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CatchTheCovid19_UWPClient.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.6*"/>
            <RowDefinition Height="0.2*"/>
            <RowDefinition Height="0.2*"/>
        </Grid.RowDefinitions>
        <TextBlock Text="바코드를 인식시켜 주세요" Grid.Row="0" FontSize="40" VerticalAlignment="Bottom" HorizontalAlignment="Center" Margin="0 0 0 50"/>
        <TextBlock Text="{Binding Name}" Grid.Row="0" FontSize="90" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0 0 0 0"/>
        <TextBlock Text="{Binding ClassRoom}" Grid.Row="1" FontSize="40" VerticalAlignment="Center" HorizontalAlignment="Center"/>
        <TextBlock Text="{Binding Promiss}" Grid.Row="2" FontSize="40" VerticalAlignment="Center" HorizontalAlignment="Center"/>
    </Grid>
</Page>
